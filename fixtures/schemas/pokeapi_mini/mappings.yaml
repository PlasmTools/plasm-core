berry_query:
  method: GET
  path:
    - type: literal
      value: api
    - type: literal
      value: v2
    - type: literal
      value: berry
    # OpenAPI path is `/api/v2/berry/` — trailing slash must match hermit's axum route.
    - type: literal
      value: ""
  pagination:
    strategy: offset
    params:
      offset: {counter: 0, step: 20}
      limit: {fixed: 20, role: page_size}
    stop_when: {field: next, absent: true}
