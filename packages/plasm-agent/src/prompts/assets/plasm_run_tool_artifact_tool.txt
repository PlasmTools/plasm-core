
**Artifact (optional):** when you need data omitted from **`content`**, use the `plasm://…` snapshot referenced by `(in artifact)` / `snapshot_only`. Call **`plasm_read_run_artifact`** with the same **`logical_session_ref`** plus `run_id` or `artifact_uri` from that step's `_meta.plasm.steps[]` (this host does not expose `resources/read`). When delivery is `inline` without those markers, rows are already in **`content`**.

The reader returns a file locator. Pass it in `plasm_artefact_transform.paths` and write a default TypeScript function over the parsed artifacts; return only the needed derived result.
