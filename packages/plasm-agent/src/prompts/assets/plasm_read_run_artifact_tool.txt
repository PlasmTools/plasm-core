Materialize a full run snapshot in the artifact workspace for programmatic processing. Pass `logical_session_ref` plus exactly one of `run_id` or `artifact_uri` from `_meta.plasm.steps[]` — not `run_step` / `dict_ref`. Returns a file locator, never snapshot contents. Pass the returned locator in `plasm_artefact_transform.paths`; write a default TypeScript function over the parsed artifacts and return only the needed derived result.
