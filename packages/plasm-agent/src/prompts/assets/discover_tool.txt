**Not the default open step.** Use **`plasm_context`** with the current **`intent`**. A bounded relevance result does not establish task completeness or catalog absence.
This browse surface is secondary diagnostics only. It does not mint program symbols or replace Jev capability matching.
