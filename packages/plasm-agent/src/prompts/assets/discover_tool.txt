**Not the default open step.** Use **`plasm_context`** with original **`intent`** plus explicit **`effect_slots`**. If matching is insufficient, report the unmatched slots; do not infer that partial teaching exists.
This browse surface is secondary diagnostics only. It does not mint program symbols or replace Jev capability matching.
