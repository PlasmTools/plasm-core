**Plan Plasm** (dry-run): **`logical_session_ref`** + **`program`**. Returns reviewable plan topology plus **`plan_commit_ref`** (`pcN`) and **`dry_verdict`** in `_meta.plasm` when review is required. Grammar and program contract are taught once in MCP initialize; symbols come from **`plasm_context`** TSV.
