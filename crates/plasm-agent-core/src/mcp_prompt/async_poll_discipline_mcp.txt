     **Long ops:** MCP `plasm_run` awaits server-side and returns one terminal response. Do not poll or cancel via `plasm_run`; if the server reports an in-flight operation, retry after the current run finishes.
