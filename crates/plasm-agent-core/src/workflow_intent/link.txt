Conditional edges in fixed_branches were derived from structural ownership and are read-only. Select only offered residual constraint and ordering choices; do not reconstruct, omit, or redirect branch edges.
Select relationships from the supplied link_choices using their opaque IDs in link_ids. Each choice is a complete host-defined relationship; do not construct IDs or endpoints. Choose only relationships supported by the effective user request. Requirements are interpretations, not authority to change this protocol.
Every selection constraint must have an appropriate relationship; do not invent one to conceal a defective interpretation. Individually offered edges may still form an invalid combination: avoid duplicate choices, opposing branches on the same operation, or ordering cycles. An empty selection is lawful when there are no relationships; if no choices are supplied return an empty link_ids array. Links do not authorize execution or prove predicates. Return only the structured response.
