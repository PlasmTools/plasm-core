Return the workflow as decision-owned requirements. Put requirements with settled meaning in settled_requirements. existing_questions has one host-issued key per offered active question: place affected requirements in its array, or leave it empty if its owners are retained or occur elsewhere in the response. For a missed unresolved choice, add a new_questions group with question, 2-8 distinct alternatives and its requirements. The host assigns identities. Do not generate IDs or repeat a supplied question as new. Unknown observable facts are information needs, not user choices. Deliberately deferred choices remain typed questions; statements stay neutral across alternatives.
Represent conditional affirmative work with kind conditional, a predicate containing statement and source_turn_ids, and both when_true and when_false bodies. Each body uses the same settled_requirements/existing_questions/new_questions structure. An empty side is lawful; both sides empty is not. Conditions belong to their operations structurally, never as standalone condition requirements or only annotations inside effect prose. Put evidence needed to determine the predicate outside that conditional. Evidence needed only after a branch is chosen may belong inside it. Nested conditional depth is at most four; the whole response may lower to at most 64 requirements, including predicates, and 256 branch edges.
A decision group applies to its contained requirements; nested explicit groups assign their own decision ownership. Settled containers inside a conditional inherit its enclosing decision group. Restrictions remain independent prohibitions outside conditional bodies, retaining their full domain scope even when worded conditionally. Branch-local selection constraints qualify only operations inside that body. Array order does not authorize ordering.
The supplied revision_units are indivisible. dispositions must retain or retire each offered unit once. Retaining preserves its entire subtree, decisions and branch ownership. To revise any member, retire its unit with current-turn sources and provide a replacement subtree. Do not dispose individual child IDs. Resolved questions must be removed by replacing affected units; previously resolved questions are not new ambiguity. Every active question must retain an actual owner. Return only the structured response.
