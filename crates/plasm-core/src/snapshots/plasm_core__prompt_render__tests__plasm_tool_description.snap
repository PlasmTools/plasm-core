---
source: crates/plasm-core/src/prompt_render/tests.rs
expression: "super::PLASM_TOOL_DESCRIPTION"
---
**Plasm** — **`logical_session_ref`** + **`program`**. Clean read-only plans execute inline (rows in tool **`content`**). Plans with any mutation / review gate return a **`run_ref`** (`pcN`, 10 min TTL) for **`plasm_run`**; do **not** echo the program.

**`program` is Plasm source text, not JSON data.** Grammar below; symbols from `plasm_context` TSV. Reply with one valid plasm_program:

**Program shape:** one `plasm_expr`, or `label = …` bindings then **one** final return line (comma-separated roots on that line only). No prose, `return`, fences, or table rows. A plan that merely echoes an object/array/string literal is a **literal no-op**.

**Entity heads vs rows:** Bare `eN` = query-all (**plural**), not “current/self”. `eN(id)` = get (**singleton**). `eN{…}` / `eN~"q"` = filtered/search (**plural**). When TSV teaches nullary `eN.mK()` providing a row, call `eN.mK()` — do not invent `eN.field` as a fetch or bind `me = eN` for that singular row. `[field,…]` keeps row shape; scalar params need `label.wire` from a **singleton** row (get / `eN.mK()` / filter+`.limit(1)`), not bare query-all. Array params may take plural `rows.wire`. After `label = eN.mK()` / `eN(id)`, use `label.wire` / `label.mK(…)`.

**Strings:** JSON escapes in quotes (`\n` `\t` `\\` `\"`); unknown `\x` errors. Prefer tagged `<<TAG` … `TAG` heredocs for long bodies (real newlines).

**Batch independent reads** (required): shared scope + multi-root return — not N tool cycles.
```
foo = e1(id="a")
bars = e2{foo=foo}
bazs = e3{foo=foo}
bars, bazs
```
Batch reads aggressively. **Prefer one multi-write program** when steps share one approval: writes run **bind-ordered (sequential)**; **one** `run_ref` for the plan. After a write use **`label.field`** (TSV wire names) — never invent a `.wire` postfix.
```
foo = e1.m1(name="n")
bar = e2.m2(foo=foo, title="t")
baz = e3.m3(bar=bar, body="…")
bar, baz
```
Worked lookup→write (illustrative mini-TSV; live symbols from `plasm_context`). **noun**↔`e#`, **verb**↔`m#`; left=code; never copy Meaning.
```tsv
plasm_expr	Meaning
e1	noun · root
m1	verb · open · provides w1
w1	str · handle from m1
e2	noun · searchable
w2	str · key on e2
e3	noun · write target
m2	verb · create linked
```
```
a = e1.m1(p1="u", p2="s")
b = e2~"q"{w1=a.w1}
c = e3.m2(w1=a.w1, w2=b.w2, n=1)
c
```

Output:
- Bind → narrow/project/transform → one return line.
- Worked transform (substitute session symbols):
items = e_source
filtered = items.filter{amount>=300}
sorted = filtered.sort(amount, desc).limit(10)
sorted[name,amount]
- Worked row-to-text (wire names in bracket and Minijinja body; source binding name also works for `{% for %}`):
report = sorted[name,amount] <<PLASM_RPT_TAG
{% for r in rows %}- {{ r.name }} ({{ r.amount or "—" }})
{% endfor %}
PLASM_RPT_TAG

TSV table semantics:
- Header `plasm_expr<TAB>Meaning`; **left column only is syntax** — never copy Meaning (arrows, `chain:`, prose).
- Executable rows start with `e#`; `r#`/`v#` are never roots.
- Meaning arrows: `↣ [e]` = plural · `→ e` = singleton · `↠` = write · `· materialize` = nullary `e.m()` yields that row (not bare `e` / `e.field`).
- Meaning arrows document result shape only. In-program: bind the write, use `label.field`. TSV `chain:` is for a **later turn** — not a ban on in-program chaining.

Core surface:
- Get: `e#(id)`. Query: `e#{field=…}`. Search when taught: `e#~"<query>"` (real query in quotes).
- Postfix from TSV left column: `.filter{…}` `.sort` `.limit` `.group_by` `.aggregate` `.with{k: expr}` `[field,…]`.
- Inline rows when delivery is `inline` (≤25); `snapshot_only` / `(in artifact)` need artifact read. Copy **`artifact_uri`** — never plan `run_step` / `dict_ref`.
- `page(...)` is HTTP-execute only — not an MCP tool argument.

Composition rules:
- One binding per line; multi-roots on the final line only.
- Row text: `label = source[field,…] <<TAG` … `TAG`; string params use **`label.content`**. Bare string bind: `label = <<TAG` … `TAG` → `param=label`.
- After `foo = e#(…)`, prefer `foo.m#(…)` / `foo.r#`. `${report.content}` in later strings (`$$` escapes `$`).
- Bare create: `e#.m#(param=…)`; scoped write: `source => e#.m#(…)`. Heredoc closes on first trimmed `TAG` line.

Symbol and fill rules:
- Code uses `e#` / `m#` / `r#`; **wire names** for fields/params/filters. Never emit `v#`.
- Homographs by syntax position (`{wire=…}`, `.r#`, `m#(wire=…)`).
- Session handles (`logical_session_ref`, `run_ref`/`pcN`) are MCP tokens — not program syntax.
- Teaching templates have holes — fill with real values; never emit leftover placeholder characters from a teaching row.
- Quote scalars in `{…}` predicates and string params. Prefer `label = e#` then `label.filter{…}` when you need the full plural set.
- Recovery: **`plasm_context`** `session_mode: "extend"` + same **`logical_session_ref`** — never **`new`**.
