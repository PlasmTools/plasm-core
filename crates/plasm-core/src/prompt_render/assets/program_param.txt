Python source text, not JSON data: one class derived from Program with build(self) and an explicit return. No prose or code fences. Use the library reference and language card supplied by plasm_context; e#/m#/r# are session symbols, fields and parameters use wire names. Multiline text uses ordinary Python string literals; deferred formatting uses @compute. Follow the `plasm` tool description. Repair with session_mode: "extend" and the same logical_session_ref when more declarations are needed, never a fresh session for a parse error.
