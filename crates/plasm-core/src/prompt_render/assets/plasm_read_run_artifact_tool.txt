Materialize a full run snapshot in the artifact workspace for programmatic processing. Pass `logical_session_ref` plus exactly one of `run_id` or `artifact_uri` from `_meta.plasm.steps[]` — not `run_step` / `dict_ref`. Returns a file locator, never snapshot contents. Use `plasm_artefact_transform` when available to process the file.
