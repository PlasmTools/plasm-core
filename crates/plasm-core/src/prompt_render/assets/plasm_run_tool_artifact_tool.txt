 **Required:** when the result includes a `plasm://…` snapshot line or `(in artifact)` cells, call **`plasm_read_run_artifact`** with the same **`logical_session_ref`** plus `run_id` or `artifact_uri` from that step's `_meta.plasm.steps[]` entry (this host does not expose `resources/read` to the model). The snapshot is the authoritative row JSON — do not infer missing fields from headers alone. Paging (`next_run_ref`) applies only when no snapshot was stored.
