 **Required:** when **`content`** includes a `plasm://…` snapshot line, `(in artifact)` cells, or **`structuredContent.plasm.result_delivery`** is `snapshot_only`, call **`plasm_read_run_artifact`** with the same **`logical_session_ref`** plus `run_id` or `artifact_uri` from that step's `_meta.plasm.steps[]` entry (this host does not expose `resources/read` to the model). The snapshot is the authoritative row JSON — do not infer missing fields from headers alone. When **`result_delivery`** is `inline`, rows are already in tool **`content`** TSV; artifact read is optional unless a snapshot line or `(in artifact)` cell is present. Paging (`next_run_ref`) applies only when no snapshot was stored.
