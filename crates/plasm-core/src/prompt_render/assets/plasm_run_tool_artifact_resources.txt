
**Artifact:** when **`content`** has a `plasm://…` snapshot, `(in artifact)` cells, or `result_delivery` is `snapshot_only`, call MCP **`resources/read`** on that URI before answering. When delivery is `inline` without those markers, rows are already in **`content`**.
