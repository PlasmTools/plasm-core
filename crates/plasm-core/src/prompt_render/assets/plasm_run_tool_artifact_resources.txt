
**Required:** when the result includes a `plasm://…` snapshot line or `(in artifact)` cells, call MCP **`resources/read`** on that URI before answering. The snapshot is the authoritative row JSON for that return step — do not infer missing fields from headers alone. Paging (`next_run_ref`) applies only when no snapshot was stored.
