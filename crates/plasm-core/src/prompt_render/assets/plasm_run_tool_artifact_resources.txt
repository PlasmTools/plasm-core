
**Artifact (optional):** when you need data omitted from **`content`**, use the `plasm://…` snapshot referenced by `(in artifact)` / `snapshot_only`. Call MCP **`resources/read`** on that URI. When delivery is `inline` without those markers, rows are already in **`content`**.
